project Atomichorsemodules is

   for Source_Dirs use ("src");
   for Object_Dir use "obj";
   for Main use ();
   for Languages use ("C", "C++");

   package Naming is
      for Spec_Suffix ("C++") use ".hpp";
   end Naming;

   package Ide is
      for Vcs_Kind use "git";
      for Vcs_Repository_Root use ".";
   end Ide;

end Atomichorsemodules;

