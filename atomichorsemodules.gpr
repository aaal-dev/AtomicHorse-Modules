project Atomichorsemodules is

   for Source_Dirs use ("src");
   for Object_Dir use "obj";
   for Main use ();
   for Languages use ("C", "C++", "Javascript");

   package Naming is
      for Spec_Suffix ("c") use ".h";
      for Spec_Suffix ("c++") use ".hpp";
      for Spec_Suffix ("javascript") use ".json";
   end Naming;

   package Ide is
      for Vcs_Kind use "git";
      for Vcs_Repository_Root use ".";
   end Ide;

end Atomichorsemodules;

